name: CBTRN03C modernization parity

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  parity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install GnuCOBOL
        run: sudo apt-get update && sudo apt-get install -y gnucobol

      - name: Build the legacy COBOL baseline (unmodified CBTRN03C)
        working-directory: legacy
        run: ./run_cbtrn03c.sh

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install modern deps
        working-directory: modern
        run: npm ci || npm install

      - name: Typecheck
        working-directory: modern
        run: npm run typecheck

      - name: Differential parity tests (modern output == COBOL baseline)
        working-directory: modern
        run: node --require ts-node/register --test test/cbtrn03c.parity.test.ts
